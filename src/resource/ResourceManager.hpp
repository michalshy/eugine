#ifndef RESOURCEMANAGER_HPP
#define RESOURCEMANAGER_HPP

class ResourceManager
{
public:
    // Constructor / Destructor
    ResourceManager();
    ~ResourceManager();
    // Interface
    bool startUp();
    bool shutDown();
private:

};

#endif